spring:
  datasource:
    url: jdbc:mysql://localhost:3306/GMK?createDatabaseIfNotExist=true&serverTimezone=UTC&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 0000
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        connection:
          CharSet: utf8
          useUnicode: true
          characterEncoding: utf-8

    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
